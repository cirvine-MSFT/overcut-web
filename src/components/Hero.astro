<section id="top" class="hero" aria-labelledby="hero-title">
	<div class="hero-media" aria-hidden="true">
		<img
			src="/images/overcut-hero.webp"
			alt=""
			width="1536"
			height="640"
			fetchpriority="high"
			decoding="async"
		/>
	</div>

	<div class="hero-shade" aria-hidden="true"></div>

	<div class="shell hero-content">
		<h1 id="hero-title">
			<span class="hero-wordmark">Overcut<sup>&trade;</sup></span>
			<span class="hero-tagline">Serious driving.<br />Shared strategy.</span>
		</h1>

		<p class="hero-copy">
			A collisionless competitive circuit racer with equal cars, distinct driver abilities,
			and a team result worth fighting for.
		</p>

		<div class="hero-actions">
			<a class="action action-primary" href="#concept-title">Meet the concept</a>
			<a class="action action-secondary" href="https://caseyirvine.dev">
				Follow the build <span aria-hidden="true">&nearr;</span>
			</a>
		</div>

		<p class="hero-status"><span aria-hidden="true"></span> Playable concept in development</p>
	</div>

	<div class="hero-rail" aria-label="Core game principles">
		<div class="shell hero-rail-inner">
			<span>Collisionless racing</span>
			<span>Equal-performance cars</span>
			<span>Driver abilities</span>
			<span>Team result</span>
		</div>
	</div>
</section>

<style>
	.hero {
		position: relative;
		isolation: isolate;
		min-height: calc(100svh - 4rem);
		overflow: hidden;
		background: var(--color-indigo);
	}

	.hero-media,
	.hero-shade {
		position: absolute;
		inset: 0;
		z-index: -2;
	}

	.hero-media img {
		display: block;
		width: 100%;
		height: 100%;
	}

	.hero-media img {
		object-fit: cover;
		object-position: 64% center;
		animation: hero-settle 1.2s cubic-bezier(0.16, 1, 0.3, 1) both;
	}

	.hero-shade {
		z-index: -1;
		background:
			linear-gradient(90deg, var(--color-night) 0%, rgb(11 14 23 / 92%) 32%, transparent 72%),
			linear-gradient(0deg, var(--color-night) 0%, transparent 34%),
			linear-gradient(180deg, rgb(11 14 23 / 35%) 0%, transparent 25%);
	}

	.hero-content {
		display: flex;
		min-height: calc(100svh - 8.4rem);
		flex-direction: column;
		align-items: flex-start;
		justify-content: center;
		padding-block: clamp(4rem, 9vw, 7rem);
	}

	.hero h1 {
		margin: 0;
	}

	.hero-wordmark,
	.hero-tagline {
		display: block;
		font-family: var(--font-display);
		font-weight: 700;
		line-height: 0.86;
		text-transform: uppercase;
	}

	.hero-wordmark {
		color: var(--color-acid);
		font-size: clamp(2rem, 4vw, 3.15rem);
		letter-spacing: 0.04em;
	}

	.hero-wordmark sup {
		margin-left: 0.12em;
		font-size: 0.22em;
		vertical-align: 2.8em;
	}

	.hero-tagline {
		max-width: 10ch;
		margin-top: 1.25rem;
		color: var(--color-paper);
		font-size: var(--text-hero);
		letter-spacing: -0.025em;
		text-wrap: balance;
	}

	.hero-copy {
		max-width: 33rem;
		margin: 1.75rem 0 0;
		color: var(--color-paper);
		font-size: clamp(1.15rem, 2vw, 1.45rem);
		line-height: 1.5;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.75rem;
		margin-top: 2rem;
	}

	.hero-status {
		display: flex;
		align-items: center;
		gap: 0.6rem;
		margin: 2rem 0 0;
		color: var(--color-paper-muted);
		font-size: 0.85rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.hero-status span {
		width: 0.6rem;
		height: 0.6rem;
		border-radius: 50%;
		background: var(--color-acid);
		box-shadow: 0 0.25rem 1rem rgb(140 255 42 / 45%);
	}

	.hero-rail {
		border-top: 1px solid color-mix(in oklab, var(--color-paper) 22%, transparent);
		background: rgb(11 14 23 / 90%);
	}

	.hero-rail-inner {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
	}

	.hero-rail span {
		padding: 0.8rem 0.75rem;
		border-right: 1px solid color-mix(in oklab, var(--color-paper) 16%, transparent);
		color: var(--color-paper-muted);
		font-family: var(--font-display);
		font-size: 0.95rem;
		font-weight: 600;
		letter-spacing: 0.07em;
		text-align: center;
		text-transform: uppercase;
	}

	.hero-rail span:nth-child(even) {
		border-right: 0;
	}

	@keyframes hero-settle {
		from {
			filter: saturate(0.85);
			transform: scale(1.025);
		}
		to {
			filter: saturate(1);
			transform: scale(1);
		}
	}

	@media (min-width: 48rem) {
		.hero-rail-inner {
			grid-template-columns: repeat(4, 1fr);
		}

		.hero-rail span:nth-child(even) {
			border-right: 1px solid color-mix(in oklab, var(--color-paper) 16%, transparent);
		}

		.hero-rail span:last-child {
			border-right: 0;
		}
	}

	@media (max-width: 47.99rem) {
		.hero {
			min-height: auto;
		}

		.hero-media img {
			object-position: 69% center;
		}

		.hero-shade {
			background:
				linear-gradient(90deg, rgb(11 14 23 / 97%) 0%, rgb(11 14 23 / 82%) 58%, transparent 100%),
				linear-gradient(0deg, var(--color-night) 0%, transparent 45%);
		}
	}
</style>
